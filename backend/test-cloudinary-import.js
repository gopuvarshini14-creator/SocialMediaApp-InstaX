const pkg = require('multer-storage-cloudinary');
console.log('Type of package export:', typeof pkg);
console.log('Package export keys:', Object.keys(pkg));
console.log('CloudinaryStorage property:', pkg.CloudinaryStorage);
